import { Fragment } from 'react';
import Foods from './SearchPageModel';

const SearchFunction = () => {
      return (
          <Fragment>
            <Foods />
          </Fragment>
      );
};

export default SearchFunction;
